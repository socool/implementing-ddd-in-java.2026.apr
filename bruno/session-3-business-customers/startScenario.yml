info:
  name: startScenario
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/startScenario"
  auth: inherit

runtime:
  variables:
    - name: API_URL
      value: http://localhost:8080

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
